---
"@farmsapp/design-system": minor
---

Add BottomBar — a `position: fixed` bar pinned to the bottom of the viewport for a screen's main actions (usually one or two full-width Buttons), with an `accessibilityLabel` naming the region for screen readers and a `zIndex` override for the sticky stacking tier.
