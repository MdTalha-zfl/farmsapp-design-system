/**
 * Box, Stack, Inline, Container, Text, Heading, Icon, VisuallyHidden land
 * here in Phase 5 — see System Blueprint's Phase 1 component list and §04.
 * Placeholder for now so the package builds, lints, and publishes
 * end-to-end.
 */
export const PRIMITIVES_PACKAGE_VERSION = "0.0.0" as const;
