/**
 * Real icon SVGs and their SVGR-generated components, plus per-icon subpath
 * exports, land here once the icon set exists. Placeholder for now so the
 * package builds, lints, and publishes end-to-end — see System Blueprint §01.
 */
export const ICONS_PACKAGE_VERSION = "0.0.0" as const;
