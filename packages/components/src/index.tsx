/**
 * Button, Input, Dialog, Menu, and the rest of the component library land
 * here starting Phase 7 — see System Blueprint §07 for the prop vocabulary
 * every component must follow. Placeholder for now so the package builds,
 * lints, and publishes end-to-end.
 */
export const COMPONENTS_PACKAGE_VERSION = "0.0.0" as const;
