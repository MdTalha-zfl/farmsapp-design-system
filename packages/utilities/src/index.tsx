/**
 * Shared hooks (useId, useFocusTrap, useControllableState) and a11y/RTL
 * helpers land here starting Phase 5 — see System Blueprint §04. Placeholder
 * for now so the package builds, lints, and publishes end-to-end.
 */
export const UTILITIES_PACKAGE_VERSION = "0.0.0" as const;
