/**
 * Re-export-only meta package — nothing is implemented here directly.
 * `export * from "@farmsapp/components"` (and primitives) lands once those
 * packages have real exports. See System Blueprint §04.
 */
export const DESIGN_SYSTEM_PACKAGE_VERSION = "0.0.0" as const;
